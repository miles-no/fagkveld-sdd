---
scope: Todo & Lists API (whole service)
purpose: build-substrate
altitude: feature
updated: 2026-09-24T17:40
---

- (direction) Fast path, spine only deliverable; consistent with PRD fast path and user's no-questions preference
- (constraint) Stack locked by PRD/krav: FastAPI, Pydantic, stdlib sqlite3, no new deps; versions ratified from repo uv.lock (fastapi 0.141.1, pydantic 2.13.5, uvicorn 0.53.0, pytest 9.1.1, httpx 0.28.1, Python 3.12)
- (decision) Paradigm layered: api -> service -> repository -> sqlite; prevents routes issuing SQL and duplicated existence checks
- (decision) Only repository touches sqlite3; one connection per request via FastAPI dependency, foreign_keys ON, commit at end of request, rollback on exception
- (decision) Not-found: repository returns None, service raises NotFoundError(entity,id), one app-level handler maps to 404 {detail}; validation stays FastAPI 422
- (decision) List delete cascades via FK ON DELETE CASCADE (inherits PRD assumption OQ-1)
- (decision) Move = PATCH /todos/{id} with list_id; single mutation path for todo; service checks target list exists
- (decision) Create todo = POST /todos with list_id in body; missing list -> 404
- (decision) Schema created idempotently at startup with CREATE TABLE IF NOT EXISTS; no migration tool
- (decision) DB path from env TODO_DB_PATH default todo.db; tests override via dependency/env with tmp path
- (decision) Integer autoincrement ids from sqlite; JSON snake_case fields id,name,title,done,list_id
- (assumption) Single local process via uvicorn; deployment/envs deferred
- (event) Spine distilled; lint clean except path-param false positives; reviewer gate skipped (low stakes, token budget); status draft pending PRD OQ-1
